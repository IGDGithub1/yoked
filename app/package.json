{
  "name": "yoked-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Yoked SPA. Builds into ../public_html/assets, which is committed and deployed as static files — there is no Node on the server.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "drive:logging": "node tests/drive-logging.mjs"
  },
  "dependencies": {
    "@fontsource-variable/bricolage-grotesque": "^5.3.0",
    "@fontsource-variable/inter": "^5.3.0",
    "lucide-react": "^1.27.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "playwright": "^1.62.0",
    "vite": "^6.0.7"
  }
}
